type User{
    _ID: ID!
    name: String!
    email: String!
    is_Active: Boolean!
}

type Query {
    users: [User]!
    user(id: ID!): User!
}

input UserInput {
    name: String!
    email: String!
    is_Active: Boolean!
}

type Mutation {
    createdUser(data: UserInput!): User!
    updateUser(id: ID! , data: UserInput!): User!
    deleteUser(id: ID!): Boolean
}